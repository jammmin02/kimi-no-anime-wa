# 君のアニメは — 구현 프롬프트 모음 (Phase 0 ~ Phase 3)

`plan_ko.md` / `plan_ja.md`(v2)에 확정된 로드맵을 그대로 따라가도록, 순서대로 붙여넣어 쓸 수 있게 프롬프트를 미리 작성했습니다. 매 프롬프트는 이전 단계의 결과물 위에 이어서 작업하도록 되어 있어서, **순서를 건너뛰지 말고 위에서부터 차례로** 사용하시면 됩니다.

사용 방법: 새 코딩 세션(Claude Code 등)을 열고, 맨 아래 "공통 규칙" 블록을 먼저 한 번 전달한 뒤(혹은 세션이 이어져 있다면 생략), 그다음 Phase 0-1부터 순서대로 프롬프트를 하나씩 넣어주세요. 한 프롬프트가 끝나고 결과를 확인한 뒤 다음 프롬프트로 넘어가는 것을 권장합니다.

---

## 공통 규칙 (모든 프롬프트에 항상 붙여서 사용)

```
프로젝트 이름: 君のアニメは

프로젝트 개요: 한국·일본 애니메이션/만화 정보를 통합해서 비교하고, 자체 서비스의 리뷰·별점
데이터를 바탕으로 AI가 맞춤 작품을 추천하는 비상업 개인/테스트 프로젝트. 원작(만화/웹툰/
라이트노벨)과 애니메이션을 연결해 "본 순서·이어볼 순서"를 안내하고, 별점 리뷰를 AI가 구간별로
요약한다. 일본어 사용자를 우선 대상으로 하되 한국어도 함께 지원한다.

기술 스택 (고정값, 임의로 바꾸거나 다른 대안으로 대체하지 말 것):
- 프론트엔드 + 백엔드: Next.js (TypeScript), 하나의 프로젝트에서 화면과 API를 함께 처리
- DB: PostgreSQL
- 배포: AWS EC2 (t3.micro/t4g.micro) + Docker Compose. 여유가 있으면 DB만 Amazon RDS
  프리티어로 분리 가능
- 인증: 이메일+비밀번호 자체(오리지널) 인증. 비밀번호는 반드시 bcrypt로 해시 저장.
  JWT 또는 세션 쿠키 사용. 소셜 로그인(카카오/네이버/구글/LINE 등)은 절대 구현하지 않음
- 번역: LibreTranslate (무료/오픈소스, 자체 호스팅)
- AI: Anthropic Claude API (Messages API) — 자연어 검색, 챗봇, 리뷰 요약, 추천 이유 생성 전부
  Claude API로 통일. 반복되는 대화/요약에는 프롬프트 캐싱 활용 고려
- 외부 데이터: AniList GraphQL API (`https://graphql.anilist.co`). 사용자 요청마다 실시간
  호출하지 말고, 반드시 배치(주기적 동기화)로 자체 DB에 캐싱한 데이터를 사용. 레이트리밋
  (기준 분당 90회, 현재 일시적으로 분당 30회일 수 있음) 준수, 429 응답 시 `Retry-After`
  헤더만큼 대기하는 로직 필수
- 이미지: 우선 AniList 원본 이미지 hotlink. S3/CloudFront 캐싱은 이후 단계 과제로만 남겨둠

랭킹 규칙 (반드시 지킬 것, 다른 방식으로 임의 변경 금지):
- 랭킹 정렬은 오직 자체 서비스의 리뷰/별점 데이터로만 계산한다. AniList의 popularity,
  averageScore/meanScore, trending은 정렬 기준으로 절대 사용하지 않고, 상세 페이지에
  "참고 정보"로만 노출한다.
- 만화 랭킹: countryOfOrigin(KR/JP) 필터 + 자체 리뷰/별점 순 정렬만 사용.
- 애니 랭킹: 장르/시즌 필터 + 자체 리뷰/별점 순 정렬만 사용. 국가별 비교는 넣지 않음
  (Phase 3 전까지는 만들지 않음).

지금 단계에서 명시적으로 만들지 말 것 (제외/보류가 확정된 항목 — 별도 지시 없이 구현하지 말 것):
- 소셜 로그인 전부
- 연령대/성별 기반 랭킹·비교 (Phase 3 이전)
- "애니가 원작 몇 권까지 다뤘는지" 자동/수동 표시 기능 (MVP 제외)
- 전자책/종이책 구분, 한국 정식 출판사·발매일 등 유통 정보
- 원작 신간 발매 자동 알림
- 자체 가입자 "국가" 필드 기준 실질 인기 비교 (Phase 3 이전)

코드 작성 공통 규칙:
- 소스 코드 안의 모든 주석(comment)은 예외 없이 100% 일본어로 작성한다. 변수/함수명은
  영어(camelCase 등 일반적인 컨벤션), UI에 노출되는 문자열은 i18n 리소스로 분리하되,
  코드 주석 자체는 반드시 일본어여야 한다. (커밋 메시지, PR 설명, 이 대화에서의 답변은
  한국어로 해도 무방함 — 오직 "코드 파일 안의 주석"만 일본어 규칙 적용)
- 이전 단계에서 만들어둔 폴더 구조·네이밍 컨벤션·코드 스타일·설정 파일을 그대로 따르고,
  새로운 패턴이나 라이브러리를 임의로 추가하지 않는다. 꼭 필요하면 왜 필요한지 먼저 설명하고
  진행할 것.
- 각 작업이 끝나면 무엇을 만들었는지, 다음 단계에서 이어받아야 할 것(예: 아직 비워둔 함수,
  TODO, 환경변수 등)을 간단히 요약해서 알려줄 것.

프론트엔드 디자인 시스템 규칙 (Phase 0-5에서 만든 것을 이후 모든 화면이 그대로 재사용):
- 스타일링: Tailwind CSS를 기본으로 하고, 반복되는 UI 요소(버튼, 카드, 인풋, 모달, 배지,
  별점, 내비게이션 바, 푸터 등)는 반드시 공용 컴포넌트로 만들어서 재사용한다. 페이지마다
  임의로 새 색상값·버튼 스타일·여백 값을 하드코딩하지 않는다.
- 디자인 컨셉: "초록이 메인인 푸른 숲(깊은 청록빛 숲)" 톤앤매너를 전체 서비스에 적용한다.
  primary는 청록빛이 도는 짙은 숲의 초록, secondary는 이끼/바위 같은 차분한 세이지그린,
  accent는 나뭇잎 사이로 비치는 햇살 같은 따뜻한 골드 계열로 정의하고, 배경/서페이스/
  보더 등 뉴트럴 색상도 순수 회색이 아니라 아주 옅은 초록빛이 도는 톤으로 통일한다.
  success 상태색은 primary와 구분되도록 primary보다 밝은 잎사귀 초록을 쓴다.
- 디자인 토큰: 색상(primary/secondary/accent/배경/텍스트/상태색), 타이포그래피 스케일,
  spacing 스케일, border-radius, 그림자(shadow) 값을 한 곳(예: tailwind.config, CSS 변수)
  에서 관리하고, 컴포넌트는 이 토큰만 참조한다.
- 폰트: 일본어 사용자가 우선 시장이므로 일본어 가독성이 좋은 폰트(Noto Sans JP 등)를
  기본으로 하고, 한국어(Noto Sans KR 등)도 자연스럽게 표시되도록 폰트 스택을 구성한다.
- 반응형: 모바일 우선(mobile-first)으로 설계하고, 표준 브레이크포인트(모바일/태블릿/데스크톱)
  를 정의해서 모든 페이지가 동일한 기준을 따르게 한다.
- 아이콘: 하나의 아이콘 세트(예: lucide-react)만 사용하고 여러 아이콘 라이브러리를 섞지 않는다.
- 톤앤매너: 애니/만화 서비스에 맞는 산뜻하고 몰입감 있는 톤(과도하게 화려하지 않게)을 유지하고,
  이후 새로 추가되는 화면도 기존 화면과 톤이 어긋나지 않는지 확인한다.
- 다크/라이트 모드를 지원하기로 했다면 P0-5에서 정한 방식을 그대로 따르고, 이후 페이지에서
  각각 색상을 다시 정의하지 않는다.
- 새 화면을 만들 때마다 "P0-5에서 정의한 컴포넌트/토큰 중 재사용 가능한 것이 있는지 먼저
  확인하고, 없을 때만 새 컴포넌트를 추가한 뒤 공용 컴포넌트 폴더에 등록한다"는 원칙을 지킨다.
```

---

## Phase 0 — 착수 전 준비

> 참고: "Anthropic API 키 발급, AWS 계정 생성/리전 선택(도쿄 `ap-northeast-1` 고려)"은
> 코딩 프롬프트가 아니라 사용자가 직접 처리해야 하는 실무이므로, 아래 프롬프트를 쓰기 전에
> 미리 준비해두세요.

### P0-1. 프로젝트 스캐폴딩

```
君のアニメは 프로젝트의 초기 스캐폴딩을 만들어줘.
- Next.js(TypeScript) 프로젝트를 새로 생성하고, App Router 기준으로 폴더 구조를 잡아줘
  (예: app/, lib/, components/, server/ 또는 팀 컨벤션에 맞는 표준 구조).
- 로컬 개발용 docker-compose.yml을 만들어서 PostgreSQL 컨테이너를 함께 띄울 수 있게 해줘.
- .env.example 파일에 필요한 환경변수 자리(DB 연결 문자열, ANTHROPIC_API_KEY,
  ANILIST_API_URL, JWT_SECRET, LIBRETRANSLATE_URL 등)를 미리 만들어줘. 실제 값은 넣지 말고
  플레이스홀더만.
- ESLint/Prettier 등 기본 코드 품질 도구를 설정해줘.
- README.md에 프로젝트 개요(君のアニメは, 한일 애니/만화 통합 추천 플랫폼)와 로컬 실행 방법을
  간단히 정리해줘. README랑 소스 코드 내 주석은 전부 일본어로 작성해줘.
```

### P0-2. DB 스키마 설계

```
Phase 0의 DB 스키마를 설계해줘. ORM은 Prisma를 사용해서(또는 이미 셋업된 방식이 있다면 그것을
따라서) 아래 테이블/모델을 만들어줘.
- 작품 마스터 테이블: AniList ID를 매핑하는 컬럼 포함, 제목(다국어), 타입(애니/만화), 장르,
  태그(연관도 %), countryOfOrigin, season/seasonYear, episodes/chapters/volumes,
  커버 이미지 URL, AniList 참고용 popularity/averageScore/trending(정렬용이 아니라
  참고 표시용이라는 것을 주석으로 명시), relations(원작-애니-속편 연결 정보)를 저장할 구조.
- 사용자 테이블: 이메일, 비밀번호 해시(bcrypt), 닉네임, 선호 언어, 취향 키워드, 국가(선택 입력,
  나중에 Phase 3에서 쓸 용도라는 것만 주석으로 남겨두고 지금은 사용하지 않음).
- 리뷰/별점 테이블: 작품 참조, 작성자 참조, 별점, 리뷰 본문, 스포일러 여부, 작성/수정 시각.
- 보관함(찜/시청 상태) 테이블: 사용자-작품-상태(찜/보는 중/완료 등).
마이그레이션 파일까지 생성하고, 스키마 파일과 마이그레이션 코드의 주석은 전부 일본어로 작성해줘.
아직 API 라우트나 화면은 만들지 말고 스키마/마이그레이션까지만 진행해줘.
```

### P0-3. AniList 배치 동기화 파이프라인

```
AniList GraphQL API(`https://graphql.anilist.co`)에서 작품 데이터를 가져와 P0-2에서 만든
작품 마스터 테이블에 채워 넣는 배치 동기화 스크립트를 만들어줘.
- title, genres, tags, countryOfOrigin, popularity, averageScore, trending, season,
  seasonYear, relations, episodes/chapters/volumes 필드를 GraphQL 쿼리로 가져와줘.
- 레이트리밋을 반드시 지키도록 구현해줘: 응답 헤더의 남은 요청 수를 확인하고, 429가 오면
  Retry-After 헤더만큼 대기 후 재시도하는 로직을 넣어줘.
- 사용자가 페이지를 열 때마다 AniList를 직접 호출하는 구조가 아니라, 이 스크립트가 주기적으로
  실행되어 DB를 갱신하는 배치 구조라는 것을 코드 상단 주석으로 명확히 남겨줘.
- 지금 단계에서는 로컬에서 수동으로 실행 가능한 스크립트(예: npm script)로만 만들고, 실제
  스케줄링(cron/EventBridge)은 P0-4에서 다룰 거야.
- 원작-애니-속편 관계(relations)는 나중에 시리즈 연결 기능(Phase 1의 3번)에서 그대로 쓸 수
  있는 형태로 저장해줘.
모든 주석은 일본어로 작성해줘.
```

### P0-4. AWS 배포 파이프라인 구성

```
君のアニメは 앱을 AWS EC2(t3.micro/t4g.micro, 프리티어 대상)에 Docker Compose로 배포하는
구성을 만들어줘.
- 프로덕션용 docker-compose.yml(또는 기존 파일 확장)에 Next.js 앱 + PostgreSQL +
  LibreTranslate 컨테이너를 함께 정의해줘. (DB를 나중에 RDS로 옮길 수 있다는 걸 감안해서
  연결 문자열은 환경변수로 분리)
- P0-3에서 만든 AniList 동기화 스크립트를 주기적으로 실행할 방법을 정해줘: EC2 내부 cron이
  더 단순하니 기본값으로 cron을 쓰고, 크론탭 설정 예시와 로그 남기는 방법을 포함해줘.
- 간단한 배포 스크립트 또는 GitHub Actions 워크플로우(원하면)로 EC2에 배포하는 절차를
  문서화해줘.
- 프로덕션 환경변수 목록을 .env.production.example로 정리해줘(실제 값 없이).
설정 파일 안의 주석, 배포 스크립트 안의 주석은 전부 일본어로 작성해줘. 문서(README 등)는
한국어로 작성해도 돼.
```

### P0-5. 프론트엔드 디자인 시스템 구축

```
君のアニメは 전체 화면에서 재사용할 프론트엔드 디자인 시스템을 만들어줘. 이 결과물은 이후
Phase 1~3의 모든 화면이 그대로 재사용할 기준이 되니, 여기서 정한 걸 이후 프롬프트에서 임의로
바꾸지 말고 계속 따라가줘.
- 전체 톤앤매너는 "초록이 메인인 푸른 숲(깊은 청록빛 숲)" 느낌으로 잡아줘. primary는
  청록빛이 도는 짙은 숲 초록, secondary는 이끼/바위 같은 차분한 세이지그린, accent는
  나뭇잎 사이로 비치는 햇살 같은 따뜻한 골드 계열로 정의해줘. success는 primary와
  구분되도록 더 밝은 잎사귀 초록으로, warning/error는 기존 색 계열을 유지하되 전체
  팔레트와 어울리게 조정해줘. 배경/서페이스/보더 같은 뉴트럴 색상도 순수 회색이 아니라
  아주 옅게 초록빛이 도는 톤(라이트 모드는 옅은 아침 안개 같은 색, 다크 모드는 깊은
  밤의 숲 같은 색)으로 통일해줘.
- Tailwind CSS를 설정하고, 위 컨셉에 맞춘 색상(primary/secondary/accent/배경/텍스트/
  성공·경고·에러 등 상태색), 타이포그래피 스케일, spacing 스케일, border-radius, shadow
  값을 디자인 토큰으로 정의해줘(tailwind.config 또는 CSS 변수 파일로 한 곳에 모아줘).
- 폰트는 일본어 사용자 우선이라는 걸 감안해서 Noto Sans JP를 기본으로 하고, 한국어(Noto Sans
  KR)도 자연스럽게 표시되도록 폰트 스택을 구성해줘. next/font 등으로 웹폰트를 최적화해서
  불러와줘.
- 공용 컴포넌트를 만들어줘: 버튼(여러 variant), 카드, 인풋/폼 필드, 모달/다이얼로그, 배지,
  별점(rating) 컴포넌트, 상단 내비게이션 바, 푸터, 로딩/스켈레톤, 빈 상태(empty state) 컴포넌트.
  모두 위에서 정의한 디자인 토큰만 참조하게 만들어줘.
- 아이콘 라이브러리는 하나만(예: lucide-react) 선택해서 설치하고, 이후 다른 아이콘 세트를
  섞어 쓰지 않는다는 규칙을 README나 코드 주석에 남겨줘.
- 모바일 우선 반응형 브레이크포인트를 정의하고, 상단 내비게이션 바와 레이아웃 컴포넌트에
  적용해서 데스크톱/모바일 모두 자연스럽게 보이는지 확인해줘.
- 다크/라이트 모드를 지원할지 여부를 정하고(추천: 라이트 모드 우선 + 다크 모드는 선택 지원),
  정한 방식을 토큰 구조에 반영해줘.
- 간단한 스타일 가이드 페이지(또는 Storybook)를 만들어서, 앞으로 새 컴포넌트를 추가할 때
  기존 토큰/컴포넌트를 재사용하고 있는지 한눈에 확인할 수 있게 해줘.
모든 주석은 일본어로 작성해줘. 이후 프롬프트(P1-1부터)는 여기서 만든 컴포넌트와 토큰을
그대로 재사용해야 하고, 페이지마다 새로운 색상이나 버튼 스타일을 임의로 만들면 안 돼.
```

---

## Phase 1 — MVP

### P1-1. 회원가입/로그인 + 취향 키워드 설정

```
이메일+비밀번호 기반 회원가입/로그인 기능을 구현해줘. 소셜 로그인은 절대 넣지 마.
- 회원가입: 이메일 중복 체크, 비밀번호 bcrypt 해시 저장, 기본 유효성 검사(이메일 형식,
  비밀번호 최소 길이 등).
- 로그인: JWT 또는 세션 쿠키 방식(팀 컨벤션이 이미 있으면 그걸 따르고, 없으면 세션 쿠키로
  시작). 로그아웃 처리 포함.
- 가입 후(또는 마이페이지에서) 취향 키워드를 선택/입력할 수 있는 화면을 만들어줘. 키워드는
  나중에 AniList의 tags 데이터와 매칭해서 추천에 쓸 거라는 전제를 코드 주석에 남겨줘.
- UI는 기본 언어를 일본어로 두고(다국어 처리는 Phase 1의 7번에서 본격적으로 다룰 예정이니,
  지금은 문자열을 하드코딩하지 말고 나중에 i18n으로 옮기기 쉽게 텍스트를 한 곳에 모아줘).
소스 코드의 모든 주석은 일본어로 작성해줘.
```

### P1-2. 작품 목록/상세 페이지

```
P0-3의 AniList 동기화로 채워진 작품 데이터를 이용해서 작품 목록 페이지와 상세 페이지를
만들어줘.
- 목록 페이지: 애니/만화 타입 필터, 장르 필터, 페이지네이션(또는 무한 스크롤).
- 상세 페이지: 제목(다국어 표기), 장르, 태그, 방영 정보(season/seasonYear), 커버 이미지,
  episodes/chapters/volumes, countryOfOrigin을 보여줘. AniList의 popularity/averageScore/
  trending은 "참고 정보" 섹션으로 명확히 구분해서 표시하고, 정렬/랭킹에는 절대 쓰지 않는다는
  점을 코드 주석에도 남겨줘.
- 아직 리뷰/별점, 랭킹, 시리즈 연결 UI는 넣지 마 (다음 프롬프트들에서 다룰 예정).
모든 주석은 일본어로 작성해줘.
```

### P1-3. 원작-애니 시리즈 연결

```
AniList의 relations 데이터(ADAPTATION, PREQUEL, SEQUEL, SIDE_STORY 등)를 활용해서, 작품
상세 페이지에 "원작 → 애니 → 속편/외전" 연결 관계를 보여주는 섹션을 추가해줘.
- P0-3에서 저장해둔 relations 데이터를 조회해서 관계 그래프(또는 순서 목록) 형태로 렌더링해줘.
- "애니가 원작 몇 권까지 다뤘는지"는 이번 프로젝트에서 자동화할 수 없는 데이터이므로 절대
  넣지 마 (MVP 제외 확정 항목).
- relations 데이터가 없는 작품은 해당 섹션을 자연스럽게 숨겨줘.
모든 주석은 일본어로 작성해줘.
```

### P1-4. 별점/리뷰 작성 기능

```
작품 상세 페이지에 별점/리뷰 작성 기능을 추가해줘.
- 로그인한 사용자만 리뷰를 작성할 수 있게 하고, 별점(예: 1~5 또는 1~10)과 리뷰 본문을
  입력받아 P0-2의 리뷰 테이블에 저장해줘.
- 스포일러 포함 여부를 체크할 수 있게 하고, 스포일러로 표시된 리뷰는 기본적으로 본문을
  가려두고 클릭해야 보이게 해줘.
- 리뷰 목록에 신고 버튼(신고 사유 선택 정도의 간단한 수준)과 댓글 기능을 추가해줘. 신고/댓글의
  관리(운영자 처리)는 Phase 1의 관리자 페이지 항목에서 다룰 수도 있으니 지금은 데이터만 잘
  쌓이게 만들어줘.
모든 주석은 일본어로 작성해줘.
```

### P1-5. 랭킹 페이지 (만화/애니)

```
만화 랭킹과 애니 랭킹 페이지를 만들어줘. 이 부분은 계획서에서 여러 번 강조된 핵심 규칙이니
정확히 지켜줘.
- 만화 랭킹: countryOfOrigin(KR/JP) 필터를 제공하고, 정렬은 오직 자체 리뷰/별점 데이터
  (평균 별점, 리뷰 수 등 우리가 정한 기준)로만 계산해줘. AniList의 popularity/averageScore는
  정렬에 절대 사용하지 마.
- 애니 랭킹: 장르/시즌 필터를 제공하고, 마찬가지로 자체 리뷰/별점 기준으로만 정렬해줘. 국가별
  비교는 넣지 마 (Phase 3 항목).
- 리뷰가 거의 없는 작품이 섞여 있을 때 랭킹이 비어 보이거나 뒤죽박죽으로 보일 수 있다는 점을
  감안해서, 최소 리뷰 수 미달 작품은 "정보 부족"으로 별도 표시하거나 랭킹 하단으로 분리하는
  방식을 제안하고 구현해줘.
모든 주석은 일본어로 작성해줘.
```

### P1-6. 취향 키워드 기반 기본 추천

```
P1-1에서 저장한 사용자 취향 키워드와 AniList tags 데이터를 매칭해서, 개인 맞춤 홈 화면에
"추천 작품" 섹션을 만들어줘.
- 아직 Claude API를 쓰는 AI 추천(Phase 2)이 아니라, 순수 콘텐츠 기반 매칭(키워드-태그
  연관도 %) 로직으로 구현해줘.
- 추천된 작품 옆에 "왜 추천됐는지"를 간단한 텍스트로 보여줘(예: 일치한 키워드 나열). 이건
  나중에 Phase 2에서 Claude API 기반으로 고도화할 자리라는 걸 주석에 남겨줘.
모든 주석은 일본어로 작성해줘.
```

### P1-7. UI 다국어 지원

```
지금까지 만든 화면 전체에 다국어(i18n) 처리를 적용해줘.
- 기본 언어는 일본어, 한국어를 함께 지원해줘. (우선 시장이 일본어 사용자이기 때문에 일본어를
  기본값으로 설정)
- 지금까지 하드코딩되어 있던 UI 문자열을 리소스 파일(예: locales/ja.json, locales/ko.json)로
  옮겨줘.
- 리뷰 본문 등 사용자 생성 콘텐츠의 번역은 이번 단계에서 다루지 말고(LibreTranslate 연동은
  Phase 2의 5번), 이번엔 UI 라벨/버튼/안내문 등 정적 텍스트의 다국어화에만 집중해줘.
모든 주석은 일본어로 작성해줘.
```

### P1-8. 보관함(찜/상태 관리) 기능

```
P0-2의 보관함 테이블을 이용해서 "찜하기", "보는 중/읽는 중", "완료" 등 상태를 관리할 수 있는
보관함 기능을 만들어줘.
- 작품 상세 페이지에서 상태를 바로 변경할 수 있는 버튼/드롭다운을 추가해줘.
- 마이페이지에 보관함 목록(상태별 필터 포함)을 보여주는 화면을 만들어줘.
모든 주석은 일본어로 작성해줘. 이 프롬프트로 Phase 1(MVP)의 마지막 항목이 끝나. 완료되면
지금까지 Phase 1 전체에서 비워둔 TODO나 다음 Phase에서 이어받아야 할 부분을 정리해서 알려줘.
```

---

## Phase 2 — AI 기능 (Phase 1 데이터/리뷰가 어느 정도 쌓인 뒤)

### P2-1. AI 자연어 검색

```
Claude API를 이용한 자연어 검색 기능을 추가해줘.
- 사용자가 자연어로("복수하는 성장형 주인공이 나오는 이세계물" 같은) 검색하면, Claude API에게
  AniList의 tags/genres 목록과 사용자의 질의를 함께 넘겨서 관련 태그/장르 조합을 추출하게 하고,
  그 결과로 자체 DB를 필터링해서 작품 목록을 보여줘.
- Claude API 키는 환경변수(ANTHROPIC_API_KEY)로 관리하고, 호출 실패/레이트리밋 시의 예외
  처리를 넣어줘.
- 검색 결과에도 랭킹 규칙(자체 리뷰 기준 정렬, AniList 인기도 미반영)을 그대로 적용해줘.
모든 주석은 일본어로 작성해줘.
```

### P2-2. AI 챗봇 추천

```
Claude API 기반의 대화형 추천 챗봇을 추가해줘.
- 사용자가 대화하듯 취향을 이야기하면, Claude가 후속 질문을 하거나 자체 DB에서 후보 작품을
  추천하도록 구현해줘.
- 반복되는 시스템 프롬프트/컨텍스트 부분은 Anthropic의 프롬프트 캐싱을 활용해서 비용을
  줄이는 구조로 만들어줘.
- 추천된 작품은 실제 존재하는 작품(자체 DB에 있는 작품)만 언급하도록, Claude 응답을 DB
  조회 결과로 검증/보강하는 로직을 넣어줘(허구의 작품을 지어내지 않도록).
모든 주석은 일본어로 작성해줘.
```

### P2-3. 별점 구간별 AI 리뷰 요약

```
작품별로 쌓인 리뷰를 별점 구간별로 Claude API가 요약해주는 기능을 추가해줘.
리뷰는 seed파일로 백개 이상 만든 후에 실행해줘
- 최소 리뷰 수(예: 20~30개 이상, 실제 값은 상수로 빼서 나중에 조정하기 쉽게)가 쌓인 작품에만
  요약을 노출하고, 미달인 작품은 "리뷰가 더 쌓이면 요약을 제공합니다" 같은 안내만 보여줘.
- 별점 구간(예: 높은 평가/중간/낮은 평가)별로 나눠서 Claude에게 요약을 요청하고, 결과를
  캐싱해서 매번 새로 호출하지 않도록 해줘(리뷰가 추가될 때만 재생성).
모든 주석은 일본어로 작성해줘.
```

### P2-4. 추천 이유 자동 생성

```
P1-6에서 만든 "추천 이유" 텍스트를, 이제 Claude API가 자동으로 자연스러운 문장으로 생성하도록
고도화해줘.
- 사용자의 취향 키워드, 일치한 태그, 해당 작품의 자체 리뷰 평균 별점 등을 Claude에게 넘겨서
  한두 문장짜리 추천 이유를 생성하게 해줘.
- 결과는 캐싱해서 같은 사용자-작품 조합에 대해 매번 새로 호출하지 않게 해줘.
모든 주석은 일본어로 작성해줘.
```

### P2-5. 리뷰 자동 번역 (LibreTranslate)

```
리뷰 본문에 대한 자동 번역 기능을 LibreTranslate로 구현해줘.
- 리뷰가 작성된 언어를 감지하고, 사용자의 화면 언어와 다르면 "번역 보기" 버튼을 눌러서
  LibreTranslate로 번역된 텍스트를 보여주는 방식으로 구현해줘.
- LibreTranslate 서버 URL은 환경변수로 관리하고, 호출 실패 시 원문을 그대로 보여주는
  fallback을 넣어줘.
모든 주석은 일본어로 작성해줘.
```

### P2-6. 랭킹 변동 추적 시작 (스냅샷)

```
매일 랭킹 스냅샷을 자체 DB에 저장하는 배치 작업을 추가해줘.
- 만화/애니 각각의 현재 랭킹(자체 리뷰 기준)을 하루 한 번 저장하는 스크립트를 만들고, P0-4의
  cron 설정에 추가해줘.
- 스냅샷이 2일 이상 쌓인 시점부터 "전일 대비 순위 상승/하락"을 랭킹 페이지에 표시해줘. 스냅샷이
  1개뿐이거나 없으면 변동 표시를 하지 않도록 처리해줘(서비스 오픈 첫날은 변동 없음이 정상).
모든 주석은 일본어로 작성해줘.
```

### P2-7. 사용자 기반(협업 필터링) 추천 추가

```
지금까지의 콘텐츠 기반 추천(P1-6, P2-4)에 더해서, 사용자 간 유사도를 이용한 협업 필터링
추천을 추가해줘.
- 리뷰/별점/보관함 데이터를 이용해 사용자-작품 상호작용 행렬을 만들고, 간단한 유사도 기반
  추천(예: 비슷한 평가를 한 사용자들이 높게 평가한 작품)을 구현해줘.
- 데이터가 적을 때는 콘텐츠 기반 추천으로 자연스럽게 fallback 되도록 해줘.
모든 주석은 일본어로 작성해줘. 완료되면 Phase 2 전체에서 남은 TODO를 정리해서 알려줘.
```

---

## Phase 3 — 커뮤니티 및 고도화 (가입자 수가 충분히 쌓인 뒤)

### P3-1. 팔로우, 리뷰 피드, 취향 일치율

```
사용자 간 팔로우 기능과 팔로우한 사용자들의 리뷰를 모아 보는 피드, 그리고 두 사용자 간 취향
일치율(취향 키워드/평가 패턴 비교)을 계산해서 보여주는 기능을 추가해줘.
모든 주석은 일본어로 작성해줘.
```

### P3-2. 작품 비교 기능

```
두 개 이상의 작품을 선택해서 나란히 비교하는 페이지를 만들어줘. 비교 항목은 자체 평균 별점,
리뷰 수, 장르/태그, 그리고 리뷰 텍스트를 Claude로 분석해서 뽑은 "자주 언급되는 키워드"로
구성해줘. 연령대/성별 비교는 아직 넣지 마(P3-4에서 다룸).
모든 주석은 일본어로 작성해줘.
```

### P3-3. 국가별 실질 인기 비교 (자체 가입자 기준)

```
지금까지는 AniList의 countryOfOrigin(원산지)만 썼는데, 이제 자체 가입자의 "국가" 필드 데이터가
충분히 쌓였다는 전제로, 가입자 국가 기준의 한국 vs 일본 실질 인기 비교 기능을 추가해줘 (애니
포함). 표본이 너무 적은 경우에는 비교를 노출하지 않는 최소 표본 기준을 두고 구현해줘.
모든 주석은 일본어로 작성해줘.
```

### P3-4. 연령대/성별 기반 랭킹

```
사용자가 선택 입력한 연령대/성별 데이터를 기반으로 한 랭킹/비교 기능을 추가해줘. opt-in 데이터
이고 응답률이 낮을 수 있으므로, 최소 표본 수 기준을 반드시 두고, 기준 미달 시에는 "데이터
부족"으로 표시하거나 섹션을 숨겨줘.
모든 주석은 일본어로 작성해줘.
```

### P3-5. "원작 몇 권까지 다뤘는지" 크라우드소싱 편집 기능

```
사용자가 "이 애니는 원작 몇 권까지의 내용을 다뤘다" 같은 정보를 제보/수정 제안할 수 있는
크라우드소싱 편집 기능을 추가해줘. 제안은 바로 반영되지 않고 관리자 페이지에서 검토/승인하는
흐름으로 만들어줘.
모든 주석은 일본어로 작성해줘.
```

### P3-6. 신작/속편/방영 알림 고도화

```
기존 방영 스케줄 알림(AniList AiringSchedule/nextAiringEpisode 기반, Phase 1 범위)에 더해서,
사용자가 보관함에 등록한 작품의 속편/신작 소식이 뜨면 알림을 보내주는 기능으로 고도화해줘.
원작 신간 발매 알림은 여전히 관련 공개 API가 없으므로 이번에도 넣지 마 (계속 보류 확정 항목).
모든 주석은 일본어로 작성해줘. 완료되면 Phase 3 전체에서 남은 TODO를 정리해서 알려줘.
```

---

## 참고

- 각 프롬프트는 앞선 단계의 산출물(테이블, 폴더 구조, 함수, 디자인 시스템 등)을 그대로
  재사용하도록 되어 있습니다. 중간에 순서를 바꾸거나 건너뛰면 뒤쪽 프롬프트에서 참조하는
  구조가 없을 수 있으니 가능하면 순서대로 실행하세요.
- 프론트엔드 디자인 일관성은 P0-5(디자인 시스템 구축)에서 한 번에 정하고, 이후 모든 Phase
  1~3 프롬프트가 그 컴포넌트/토큰을 재사용하는 구조로 짜여 있습니다. 새 화면을 만들 때마다
  에이전트가 임의로 새 색상/버튼 스타일을 만들지 않는지 한 번씩 확인하세요.
- 계획서(v2)에서 이미 "제외/보류"로 확정된 기능들은 모든 프롬프트에서 공통적으로 금지 항목에
  넣어뒀습니다. 혹시 계획이 바뀌면 이 문서도 함께 업데이트가 필요합니다.
- 코드 주석을 일본어로 쓰라는 지시는 모든 프롬프트에 반복해서 넣어뒀지만, 실제로 에이전트가
  이 규칙을 놓칠 수도 있으니 작업 후 결과물의 주석 언어를 한 번씩 확인하시는 걸 권장합니다.
