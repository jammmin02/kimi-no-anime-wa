# 한·일 애니·만화 통합 추천 플랫폼 — 기획 정리 및 구현 로드맵 (v2)

> 원본 기획안(9장 구성)을 바탕으로, **AniList API(https://anilist.co)를 실제 데이터 소스로 사용한다는 전제** 하에 기능별 실현 가능성을 검토하고, 실행 불가능하거나 재정의가 필요한 기능을 정리했습니다. v1에서 제시했던 10가지 결정 사항에 대한 답변을 반영해, 이번 v2에서는 랭킹 방식·로그인 방식·번역/AI·인프라·우선 시장을 모두 확정된 값으로 갱신했습니다.

---

## 0. v1 → v2 변경 요약

이 프로젝트는 **비상업적 개인/테스트 프로젝트**로 진행하기로 확정되었습니다. 이에 따라 다음 항목들이 v1과 달라졌습니다.

- 랭킹: AniList 글로벌 인기도를 "참고 랭킹"으로 병기하던 방식을 없애고, **오직 자체 서비스의 리뷰·별점 기준 랭킹만** 사용합니다. AniList 데이터는 목록을 채우고 필터링(원산지 등)하는 용도로만 쓰입니다.
- 로그인: 소셜 로그인(카카오/네이버/구글/LINE 등)을 붙이지 않고, **이메일+비밀번호 기반의 자체(오리지널) 회원 시스템**만 구현합니다.
- 번역: 유료 API 대신 **무료 번역 엔진(LibreTranslate)**을 기본값으로 채택합니다.
- AI: 자연어 검색/챗봇/리뷰 요약 모두 **Claude API(Anthropic)**를 사용합니다.
- 인프라: **AWS**로 확정. 나머지 기술 스택(프레임워크, DB, 배포 구조)은 제가 이 프로젝트 규모에 맞춰 제안합니다.
- 우선 시장: **일본어 사용자 중심**으로 화면 기본 언어와 초기 콘텐츠 우선순위를 잡습니다.
- 상업적 이용, 이미지 저작권, 개인정보 처리에 대한 무거운 법률 검토는 비상업 프로젝트이므로 생략하되, 최소한의 보안 관행(비밀번호 해시 저장 등)은 유지합니다.

이 외에 "AniList는 국가별 인기 데이터를 제공하지 않는다"는 v1의 핵심 조사 결과와, 그에 따른 3장의 기능별 유지/재정의/제외 판단은 그대로 유효합니다.

---

## 1. 프로젝트 한 줄 요약

한국과 일본의 애니메이션·만화 정보를 한곳에 모아 비교하고, 사용자의 취향과 리뷰 데이터를 바탕으로 AI가 맞춤 작품을 추천하는 개인 프로젝트. 원작(만화/웹툰/라이트노벨)과 애니메이션을 연결해 "본 순서, 이어볼 순서"를 알려주고, 별점 리뷰를 AI가 구간별로 요약해준다. 일본어 사용자를 우선 대상으로 하되 한국어도 함께 지원한다.

작품 메타데이터(제목, 장르, 방영 정보, 원작-애니 연결 관계, 커버 이미지 등)는 AniList API에서 가져와 자체 DB에 주기적으로 캐싱하고, **랭킹·리뷰·추천은 전적으로 우리 서비스 자체 사용자 데이터를 기반으로 만든다.**

---

## 2. AniList API로 할 수 있는 것 / 할 수 없는 것

### 2.1 AniList가 제공하는 데이터 (확인됨)

| 항목 | 필드 | 활용 가능성 |
|---|---|---|
| 제목(로마자/영어/원어) | `title` | 다국어 제목 표기에 활용 가능 (단, 한국어 공식 제목은 없음) |
| 장르 | `genres` | 장르별 분류/검색에 바로 활용 |
| 태그(분위기, 소재 등 세부 태그 + 연관도 %) | `tags` | "성장형 주인공", "복수" 같은 취향 키워드 매칭에 매우 유용 |
| 원산지 국가 | `countryOfOrigin` | 만화(웹툰) 한국산/일본산 구분에 활용 (만화 랭킹의 "한국/일본" 분류 기준으로 확정 사용) |
| 글로벌 인기도 | `popularity` | 전 세계 기준 인기 지표. **랭킹 정렬에는 사용하지 않고**, 참고용 메타데이터로만 상세 페이지에 표시 |
| 평균 평점 | `averageScore`, `meanScore` | 위와 동일하게 참고용으로만 표시 (자체 랭킹 정렬 기준 아님) |
| 트렌딩 지수 | `trending` | 신작 발견용 "지금 화제작" 섹션 정도로만 활용 (랭킹 정렬 기준 아님) |
| 방영 시기/시즌 | `season`, `seasonYear` | 분기별(2026년 1~4분기) 신작 분류에 그대로 활용 |
| 방영 스케줄 | `AiringSchedule`, `nextAiringEpisode` | 방영 예정/신작 알림 기능에 활용 가능 |
| 원작-애니-속편 관계 | `relations` (ADAPTATION, PREQUEL, SEQUEL, SIDE_STORY 등) | **3.3 시리즈 연결 기능의 핵심 데이터. 매우 유용** |
| 권수/화수 | `episodes`, `chapters`, `volumes` | 분량 정보 제공 가능 |

### 2.2 AniList가 제공하지 않는 데이터 (조사 결과)

- 국가별(한국/일본) 이용자 인기 순위, 국가별 평점 — **없음**
- 연령대별, 성별 인기 순위 — **없음**
- 순위의 과거 이력(어제 대비 상승/하락) — **없음** (우리가 직접 매일 스냅샷을 쌓아야 계산 가능)
- 애니메이션이 원작 몇 권 분량까지 다뤘는지 — **없음**
- 한국 정발 출판사/발매일 등 한국 시장 유통 정보 — **없음/매우 불완전**
- 전자책/종이책 구분 — **없음** (형식 필드는 만화/소설/단편 정도만 구분)

### 2.3 이용 조건 관련 확인 사항 (개인/테스트 프로젝트 기준)

- 요청 빈도 제한: 분당 90회가 기본값이나, 현재(문서 조사 시점) 일시적으로 분당 30회로 낮아져 있음. 429 응답 시 `Retry-After` 헤더만큼 대기해야 함. 이는 상업적 이용 여부와 무관하게 **기술적으로 반드시 지켜야 하는 제약**이라 그대로 유지합니다.
- 상업적 이용/재배포 세부 약관은 이 프로젝트가 판매·수익화 목적이 아니므로 별도로 AniList 측에 문의하지 않고 진행합니다. 다만 나중에 프로젝트를 공개 서비스나 상업적 용도로 전환하게 되면, 그 시점에 반드시 AniList 이용약관을 재확인해야 합니다.
- 사용자 페이지 요청마다 AniList를 실시간 호출하지 않도록, 여전히 **배치(주기적 동기화) 방식으로 자체 DB에 캐싱**하는 구조를 유지합니다 (테스트 프로젝트라도 요청 제한에 걸리면 개발 자체가 막히기 때문).

---

## 3. 기능별 실현 가능성 재검토

### 3.1 그대로 유지 가능한 기능

- 회원가입/로그인(이메일+비밀번호 기반 자체 시스템, 소셜 로그인 없음), 취향 키워드 설정 (3.4)
- 개인 맞춤 홈, 추천 이유 표시 (3.5) — 자체 추천 로직
- AI 자연어 검색 (3.6) — Claude API + AniList의 `tags`/`genres` 데이터를 조합
- AI 챗봇 추천 (3.7) — Claude API 기반
- 별점/리뷰 작성, 스포일러 가리기, 댓글/신고 (3.8)
- 별점 구간별 AI 리뷰 요약 (3.9) — Claude API 기반. **최소 리뷰 수(예: 20~30개 이상)가 쌓인 작품만** 요약 노출
- 다국어 UI(기본값 일본어) 및 리뷰 번역 (3.10) — 자체 i18n + LibreTranslate 조합
- 원작-애니-속편-외전 시리즈 연결 (3.3의 시리즈 연결 부분) — AniList `relations` 데이터로 상당 부분 자동화
- 방영 시즌별 분류, 신작/방영 알림 — AniList 시즌 필드 및 방영 스케줄 데이터로 구현
- 보관함(찜/시청 상태 관리), 작품 비교, 팔로우/리뷰 피드 — 전부 자체 DB 기능

### 3.2 "재정의"해서 유지하는 기능

| 원본 기능 | 문제 | 재정의 방향 (확정) |
|---|---|---|
| 한국 만화 순위 / 일본 만화 순위 (3.1) | "국가별 인기"가 아니라 "원산지" 데이터만 있음 | countryOfOrigin=KR / JP로 목록을 나눈 뒤, **오직 우리 서비스 자체 리뷰·별점 순으로만 정렬**한다. AniList 인기도·평점은 정렬에 반영하지 않고 상세 페이지 참고 정보로만 노출 |
| 국가별 비교표 (평균 별점, 주요 연령대, 키워드 등) | 연령대·성별 데이터 자체가 없음 | 초기에는 "자체 평균 별점, 리뷰 수, 자주 언급된 키워드(리뷰 텍스트 Claude 분석)"만 비교 항목으로 제공. 연령대/성별 비교는 자체 유저 데이터가 충분히 쌓인 뒤(3단계)로 이동 |
| 순위 상승/하락 표시 | AniList에 이력 데이터 없음 | 서비스 오픈 이후 **매일 자체 DB에 랭킹 스냅샷을 저장**하기 시작해서, 그 시점부터의 변동만 계산해서 보여준다 (오픈 첫날은 변동 없음) |
| 애니메이션 국가 랭킹 비교 (3.2) | 애니는 99% 원산지가 일본이라 원산지 기준 구분이 무의미 | 원산지 기준 비교는 포기하고, **우리 서비스 가입자의 "국가" 필드 기준 인기 순위**로 대체. 가입자 수가 쌓여야 의미가 생기므로 로드맵 3단계로 이동 |

> 참고: 랭킹을 오직 자체 리뷰 기준으로만 정렬하기로 했기 때문에, **서비스 오픈 초기에는 리뷰가 거의 없어 랭킹이 비어 있거나 뒤죽박죽으로 보일 수 있습니다.** 5장의 "초기 시드 데이터 준비" 항목이 이 문제를 완화하는 방법입니다.

### 3.3 제외하거나 보류하는 기능 (이유 포함)

| 기능 | 제외/보류 이유 | 비고 |
|---|---|---|
| 연령대별·성별 인기 순위 (3.1, 3.2) | AniList/외부 공개 데이터에 전혀 없음. 자체 가입자 데이터로만 가능한데 나이·직업은 "선택 입력"이라 응답률이 낮을 가능성이 큼 | 3단계로 이동, 최소 표본 확보 전에는 노출 안 함 |
| 애니메이션이 원작 몇 권까지 다뤘는지 / 몇 권부터 이어봐야 하는지 (3.3) | 자동화할 수 있는 데이터 소스가 없음. 작품마다 사람이 직접 확인해서 입력해야 하는 순수 수작업 | MVP 제외. 이후 "정보 제보/수정 제안" 크라우드소싱 편집 기능으로 3단계에서 검토 |
| 전자책/종이책 구분, 한국 정식 출판사·발매일 정보 (3.1) | AniList에 없고, 국내 플랫폼(카카오페이지, 네이버시리즈 등) 정보를 가져오려면 별도 제휴 또는 크롤링이 필요 | 개인 프로젝트 범위에서는 우선순위 낮음. MVP 제외 |
| "일본에서는 유명하지만 한국에서는 덜 알려진 작품" 자동 탐지 (3.6 검색 예시) | 국가별 인기 데이터가 없어 자동 계산 불가 | 자체 데이터가 충분히 쌓이기 전까지는 "데이터 부족" 안내만 제공하거나 기능 자체를 숨김 |
| 원작 신간 발매 자동 알림 (4장 알림) | 출판사별 발매 일정을 실시간으로 주는 공개 API가 없음 | 방영 스케줄 알림만 MVP에 포함, 출판 알림은 보류 |
| 소셜 로그인(카카오/네이버/구글/LINE 등) | **확정 답변에 따라 제외** — 오리지널(이메일+비밀번호) 로그인만 구현 | 추후 필요하면 언제든 추가 가능한 구조로만 설계해둠 |

---

## 4. 확정된 결정 사항 (v1의 10개 질문에 대한 답변)

| # | 질문 | 확정 답변 | 반영 내용 |
|---|---|---|---|
| 1 | AniList 이용 범위 | 비상업적 개인/테스트 프로젝트라 신경 쓰지 않아도 됨 | AniList에 별도 문의 없이 진행. 단, 향후 상업화 시에는 반드시 재확인 필요 (2.3 참고) |
| 2 | 랭킹의 정의 | 우리 서비스 리뷰 기준 자체 랭킹만 표시 | AniList 인기도는 랭킹 정렬에서 제외, 참고 메타데이터로만 노출 (3.2 참고) |
| 3 | 한국/일본 랭킹의 의미 (만화) | 원산지(countryOfOrigin) 기준 | 그대로 확정 (3.2 참고) |
| 4 | 번역 API | 무료 API | **LibreTranslate**(오픈소스, 자체 호스팅형 무료 번역 엔진)를 기본값으로 채택. 품질은 유료 서비스(DeepL 등)보다 낮을 수 있음을 감안 |
| 5 | AI(LLM) 제공자 | Claude API | 자연어 검색, 챗봇, 리뷰 요약, 추천 이유 생성까지 전부 Claude API(Anthropic Messages API)로 통일 |
| 6 | 소셜 로그인 범위 | OAuth 소셜 로그인 없이 오리지널 로그인만 | 이메일+비밀번호 자체 인증(비밀번호는 반드시 해시 저장) |
| 7 | 기술 스택/인프라 | 인프라는 AWS, 나머지는 자유 재량 | 아래 4.1 참고 — 개인 프로젝트 규모에 맞춘 경량 스택 제안 |
| 8 | 이미지 저작권 처리 | 비상업이라 괜찮음 | AniList 커버 이미지 hotlink로 우선 시작 (필요 시 이후 S3 캐싱으로 전환) |
| 9 | 개인정보 처리 범위 | 비상업이라 괜찮음 | 무거운 법률 문서 대신, 비밀번호 해시·최소 수집 원칙 정도의 기본 보안만 유지 |
| 10 | 초기 우선 시장 | 일본어 사용자 중심 | UI 기본 언어를 일본어로 설정, 콘텐츠/번역 우선순위도 일본어 쪽을 먼저 다듬음 |

### 4.1 제안 기술 스택 (7번 항목 상세)

개인/테스트 프로젝트 규모와 "인프라는 AWS"라는 조건에 맞춰, 비용과 운영 부담이 작은 구성을 제안합니다.

- **프론트엔드 + 백엔드**: Next.js (TypeScript) — 하나의 프로젝트에서 화면과 API를 함께 처리해 개인 프로젝트에 적합
- **DB**: PostgreSQL — 초기에는 EC2 한 대 안에 Docker로 함께 띄우거나, 여유가 되면 Amazon RDS 프리 티어(db.t3.micro/t4g.micro) 사용
- **배포**: AWS EC2(t3.micro/t4g.micro, 프리 티어 대상) 위에 Docker Compose로 Next.js 앱 + PostgreSQL + LibreTranslate 컨테이너를 함께 운영
- **이미지**: 우선 AniList 원본 이미지를 hotlink, 트래픽이 늘어나면 S3 + CloudFront로 캐싱 전환
- **AniList 배치 동기화**: AWS EventBridge(스케줄) + Lambda, 또는 간단히 EC2 내 cron으로 시작 (인프라 단순화가 우선이면 cron이 더 쉬움)
- **인증**: 자체 이메일/비밀번호 + JWT 또는 세션 쿠키 (bcrypt로 비밀번호 해시)
- **AI**: Anthropic Claude API — 특히 같은 대화가 반복되는 챗봇/요약 기능에서는 프롬프트 캐싱을 활용하면 개인 프로젝트 규모의 비용을 크게 줄일 수 있음

---

## 5. 사용자(기획자)가 직접 처리해야 할 실무 (To-do)

- Anthropic Claude API 키 발급 및 예산 한도(월 사용량 제한) 설정
- AWS 계정 생성, 프리 티어 한도 확인, 리전 선택(일본 사용자 우선이므로 도쿄 리전 `ap-northeast-1` 고려)
- 서비스명·도메인 확정 (상표 확인은 비상업 개인 프로젝트라 필수는 아니지만, 나중에 공개할 계획이 있다면 미리 확인해두는 편이 안전)
- 초기 콜드스타트용 시드 데이터 준비: 오픈 초기엔 리뷰가 없으므로, 본인이 직접 주요 인기작 일부에 리뷰/설명을 선 등록해두는 계획 수립 (랭킹이 비어 보이는 문제 완화)
- 개발 일정 계획 (혼자 진행할지, 몇 명이 함께할지에 따라 Phase별 기간 조정)
- 간단한 디자인 방향(색상/톤) 결정 — 필요하면 도와드릴 수 있음

---

## 6. 구현 로드맵

### Phase 0 — 착수 전 준비
- Anthropic API 키, AWS 계정 준비
- 자체 DB 스키마 설계: 작품 마스터 테이블(AniList ID 매핑 포함), 사용자 테이블, 리뷰/별점 테이블
- AniList → 자체 DB 배치 동기화 파이프라인 설계 (주기적 동기화, rate limit 준수)
- Next.js + PostgreSQL 기본 프로젝트 셋업, AWS EC2에 배포 파이프라인 구성

### Phase 1 — MVP
1. 회원가입/로그인(이메일+비밀번호) + 취향 키워드 설정
2. AniList 데이터 배치 동기화 → 작품 목록/상세 페이지 (제목, 장르, 태그, 방영정보, 커버 이미지, countryOfOrigin)
3. 원작-애니 시리즈 연결 (AniList `relations` 활용, "몇 권까지 다뤘는지"는 제외)
4. 별점/리뷰 작성 기능 (스포일러 가리기 포함)
5. 랭킹: 만화는 원산지(KR/JP) 필터 + 자체 리뷰/별점 순 정렬만 사용. 애니는 장르/시즌 필터 + 자체 리뷰 순 정렬 (AniList 인기도는 랭킹에 미반영)
6. 취향 키워드 기반 콘텐츠 기반 기본 추천
7. UI 다국어 지원 (기본 언어: 일본어, 한국어 병행)
8. 보관함(찜/상태 관리) 기능

### Phase 2 — AI 기능 (기본 데이터/리뷰가 어느 정도 쌓인 뒤)
1. AI 자연어 검색 (Claude API + AniList 태그 데이터)
2. 취향 기반 AI 챗봇 (Claude API)
3. 별점 구간별 AI 리뷰 요약 (Claude API, 최소 리뷰 수 도달 작품부터)
4. 추천 이유 자동 생성 (Claude API)
5. 리뷰 자동 번역 (LibreTranslate)
6. 랭킹 변동 추적 시작 (매일 스냅샷 저장 → 이후 상승/하락 표시 가능)
7. 사용자 기반(협업 필터링) 추천 추가

### Phase 3 — 커뮤니티 및 고도화 (가입자 수 충분히 확보된 뒤)
1. 팔로우, 리뷰 피드, 취향 일치율
2. 작품 비교 기능
3. 자체 가입자 "국가" 필드 기준 한국 vs 일본 실질 인기 비교 (애니 포함)
4. 연령대/성별 기반 랭킹 (opt-in 데이터 기반, 표본 충분할 때만)
5. "원작 몇 권까지 다뤘는지" 크라우드소싱 편집 기능
6. 신작/속편/방영 알림 고도화

---

## 7. 조정된 페이지 구성

| 페이지 | 주요 내용 (조정 반영) |
|---|---|
| 홈 | 개인 맞춤 추천, 인기 작품(자체 리뷰 랭킹 기준), 신작 |
| 애니 랭킹 | 장르/시즌 필터 + 자체 리뷰 순 랭킹 (국가별 비교는 3단계 이후) |
| 만화 랭킹 | 원산지(한국/일본) 필터 + 자체 리뷰 순 랭킹 |
| AI 검색 | Claude 기반 자연어 검색 (Phase 2) |
| AI 챗봇 | Claude 기반 대화형 추천 (Phase 2) |
| 작품 상세 | 기본 정보, 원작-애니 연결, 리뷰, AI 요약(리뷰 충분 시), AniList 참고 지표(인기도/평점 — 정렬용 아닌 정보용) |
| 작품 비교 | Phase 3 |
| 보관함 | 찜/감상 기록 관리 |
| 취향 분석 | Phase 3 (데이터 충분 시) |
| 마이페이지 | 프로필, 리뷰, 찜, 설정 |
| 관리자 페이지 | 작품 동기화 상태, 리뷰/신고 관리, 시드 리뷰 등록, 시리즈 연결 수작업 편집 |

---

## 8. 부록 — AniList API 참고 정보

- GraphQL 엔드포인트: `https://graphql.anilist.co`
- 요청 제한: 분당 90회(기준), 현재 일시적으로 분당 30회로 제한 중일 수 있음. `X-RateLimit-Remaining` 헤더로 잔여량 확인, 429 발생 시 `Retry-After` 헤더만큼 대기. (개인 프로젝트라도 이 제한은 그대로 적용되므로 배치 캐싱 구조는 유지)
- 활용 핵심 필드: `title`, `genres`, `tags`, `countryOfOrigin`, `popularity`, `averageScore`, `trending`, `season`/`seasonYear`, `relations`, `episodes`/`chapters`/`volumes`, `AiringSchedule`
- 상업적 이용/장기 캐싱/이미지 재배포 관련 세부 약관은 이번 프로젝트에서는 확인하지 않기로 함 (비상업 목적). 추후 공개·상업 서비스로 전환 시 재확인 필요
